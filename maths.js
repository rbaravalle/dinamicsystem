
function aleat(x) {
    return Math.floor(Math.random()*x);
}

function sign() {
    if(Math.random() > 0.5) return -1;
    return 1;
}

function toSpace(x) {
    return x*(diffX/maxcoord)+x0;
}


function aCanvas(x) {
    return Math.floor(x*maxcoord);
}

